// Write a JS function that rounds numbers to a specific precision. Remove trailing zeroes.

function precision (num, precision) {
    if (precision > 15) {
        precision =15;
    }
    let fixedNum = num.toFixed (precision);
    console.log (parseFloat (fixedNum));

} precision (3.1415926535897932384626433832795,2);
